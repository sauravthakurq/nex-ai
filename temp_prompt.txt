The user's HTML has base-element-* and styling. Should I convert this HTML precisely into JSX? Yes. I'll make a Next.js client component that returns exactly this HTML as JSX. The only thing I need to do is convert inline `style="..."` to React `style={{}}` format and `class` to `className`.
